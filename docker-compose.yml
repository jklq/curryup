version: "3.8"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RUNNER_SERVICE_URL=http://runner:8080
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
    depends_on:
      - runner
  runner:
    build: ./runner-service
    ports:
      - "8080:8080"
